import 'package:flutter/material.dart';

class TaskScreen extends StatelessWidget {
  const TaskScreen({
    super.key,});

  @override
  Widget build(BuildContext context) {
    // Xây dựng giao diện dựa trên state
    return const Scaffold(
        body: Text("TaskScreen")
    );
  }
}